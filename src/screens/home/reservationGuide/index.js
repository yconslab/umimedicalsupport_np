import React from "react";
import { Text, View } from "react-native";

import { Modals } from "../commons";
const ReservationGuide = ({ params }) => (
  <View style={{ flex: 1, backgroundColor: "red" }}>
    <Modals />
  </View>
);

export default ReservationGuide;
